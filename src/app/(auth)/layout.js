import Image from "next/image";

export default function AuthLayout({ children }) {
    return (

        <div >

            {/* Render Signin or Signup page here */}
            {children}
        </div>
    )
}
